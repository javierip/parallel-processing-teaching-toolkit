cmake_minimum_required( VERSION 2.6 )
project( application-MPI )

find_package( MPI REQUIRED )

include_directories( ${MPI_INCLUDE_PATH} )

add_executable( application-MPI main.c )
target_link_libraries( application-MPI ${MPI_LIBRARIES} )

